
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home</title>
  <link rel="stylesheet" href="/styles/Home.css" />
  <script>
  const currentUserId = {{user.id}};
 let currentXP = {{user.xp}};
let currentLevel = {{user.level}};
</script>
  <script src="/scripts/Home.js" defer></script>
</head>
<body>

  <div class="container">
    <div class="character-section">
      <h1>Welcome, {{user.charactername}}</h1>
      
      <div class="character-info">
        <img src="/img/malePixel.png" alt="Your character" class="character-image">
      </div>
    </div>

    <div class="task-section">

      <div class="task-header">
        <h2>Active tasks</h2>
        <div class="level-info">Level: {{currentLevel}}</div>
       <div class="xp-bar-wrapper">
          <div class="xp-bar-container">
            <div class="xp-bar-fill" id="xpFill"></div>
          </div>
          <span class="xp-text" id="xpInfo">XP: 0 / 100</span>
        </div>
      </div>

      {{#if tasks.length}}
        <div class="task-list">
          {{#each tasks}}
            <div class="task-card">
              <div class="task-icon">{{title.[0]}}</div>
              <div class="task-info">
                <div class="task-title">{{title}}</div>
                <div class="task-exp">{{xp}} xp points</div>
                <div class="task-date">Due: {{dueDate}}</div>
              </div>
              <div class="task-expand">&#9660;</div>
            </div>
            <div class="task-details hidden">
              <p><strong>Description:</strong> {{description}}</p>
              <button class="complete-btn" data-xp="{{xp}}">Complete</button>
            </div>
          {{/each}}
        </div>
      {{else}}
        <p>You have no active tasks. 
        <a href="/Taskmanager" class="create-task-btn">Create one here</a>
        </p>
      {{/if}}
    </div>
  </div>
</body>
</html>


